// This is a dummy configuration file to satisfy the ESLint requirement for a config file.
// The only reason to include it is to be able to run ESLint lint-staged tasks from the root of the monorepo.
// The monorepo doesn't have a global ESLint configuration file and each package has its own ESLint configuration.
export default {};
